import React from 'react';

const CreateContact = () => {
    return (
        <button type="button" className="btn btn btn-info">
            Create Contact
        </button>
    );
};

export default CreateContact;
